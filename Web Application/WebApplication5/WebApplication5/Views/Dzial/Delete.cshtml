@model WebApplication5.Models.Dzial

@{
    ViewData["Title"] = "Delete";
}
@if (User.Identity.IsAuthenticated && User.Claims.FirstOrDefault(c => c.Type == "Role")?.Value == "Prezes")
{
    <h1>Delete</h1>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Dzial</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.NazwaDzial)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.NazwaDzial)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="IdDzial" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-action="Index">Back to List</a>
        </form>
    </div>
}