@model WebApplication5.Models.Dzial

@{
    ViewData["Title"] = "Details";
}

@if (User.Identity.IsAuthenticated && User.Claims.FirstOrDefault(c => c.Type == "Role")?.Value == "Prezes" || User.Claims.FirstOrDefault(c => c.Type == "Role")?.Value == "Specjalista")
{
    <h1>Details</h1>

    <div>
        <h4>Dzial</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.IdDzial)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.IdDzial)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.NazwaDzial)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.NazwaDzial)
            </dd>
        </dl>
    </div>
    <div>
        @if (ViewData["Rola"].ToString() == "Prezes")
        {
            <a asp-action="Edit" asp-route-id="@Model.IdDzial">Edit</a>
        }
        <a asp-action="Index">Back to List</a>
    </div>
}